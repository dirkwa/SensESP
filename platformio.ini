; PlatformIO Project Configuration File
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[platformio]

; Change the default environment to match your board. Valid options are:
; - arduino_esp32
; - pioarduino_esp32
; - espidf_esp32
; - pioarduino_esp32c3
; - espidf_esp32c3
; - shesp32
; - halmet
; - halser
; - olimex_esp32_poe_iso  (Ethernet + PoE)
; - wt32_eth01            (Ethernet)
; - signalk_ble_gateway   (Ethernet + BLE, Signal K BLE gateway)

default_envs = pioarduino_esp32

[env]

; Options common for all working environments

;; Uncomment and change these if PlatformIO can't auto-detect the ports
;monitor_port = /dev/tty.usbserial-310
;upload_port = /dev/tty.usbserial-310
upload_speed = 2000000
monitor_speed = 115200

lib_deps =
    mairas/ReactESP @ ^3.2.0
    bblanchon/ArduinoJson @ ^7.0.0
    pfeerick/elapsedMillis @ ^1.0.6
    bxparks/AceButton @ ^1.10.1
    ; Add any additional dependencies here
    ;ttlappalainen/NMEA2000-library
    ;NMEA2000_twai=https://github.com/skarlsson/NMEA2000_twai

build_flags =
    ; Max (and default) debugging level in Arduino ESP32 Core
    -D CORE_DEBUG_LEVEL=ARDUHAL_LOG_LEVEL_VERBOSE
    ; Use the ESP-IDF logging library - required by SensESP.
    -D USE_ESP_IDF_LOG

; This line defines the partition table to use. "min_spiffs" has two app
; partitions, one for OTA updates and one for the running app, but the
; SPIFFS filesystem is quite small. For 8 MB flash boards such as HALMET,
; you can use "default_8MB.csv" instead.

board_build.partitions = min_spiffs.csv

;; Uncomment the following lines to use Over-the-air (OTA) Updates
;upload_protocol = espota
;upload_port = IP_ADDRESS_OF_ESP_HERE
;upload_flags =
;  --auth=YOUR_OTA_PASSWORD

; You shouldn't need to touch the settings in this section below this line.

build_unflags =
    -Werror=reorder
monitor_filters = esp32_exception_decoder

test_build_src = true
check_tool = clangtidy
check_flags =
    clangtidy: --fix --format-style=file --config-file=.clang-tidy


;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
; Platform configurations follow

[arduino]

platform = espressif32 @ ^6.9.0
framework = arduino

lib_ignore =
    esp_websocket_client

build_flags =
    ${env.build_flags}
    ; Arduino Core bug workaround: define the log tag for the Arduino
    ; logging macros.
    -D TAG='"Arduino"'

[pioarduino]

platform = https://github.com/pioarduino/platform-espressif32/releases/download/stable/platform-espressif32.zip
framework = arduino

; The library.json format doesn't support dependencies conditional on the
; platform version, so we have to use the lib_deps option to specify the
; esp_websocket_client library only for the ESP-IDF framework.

lib_deps =
    ${env.lib_deps}
    esp_websocket_client=https://components.espressif.com/api/downloads/?object_type=component&object_id=dbc87006-9a4b-45e6-a6ab-b286174cb413

build_flags =
    ${env.build_flags}
    -D SENSESP_SSL_SUPPORT=1

[espidf]

platform = https://github.com/pioarduino/platform-espressif32/releases/download/stable/platform-espressif32.zip
framework = espidf, arduino

board_build.embed_txtfiles =
    managed_components/espressif__esp_insights/server_certs/https_server.crt
    managed_components/espressif__esp_rainmaker/server_certs/rmaker_mqtt_server.crt
    managed_components/espressif__esp_rainmaker/server_certs/rmaker_claim_service_server.crt
    managed_components/espressif__esp_rainmaker/server_certs/rmaker_ota_server.crt

; The library.json format doesn't support dependencies conditional on the
; platform version, so we have to use the lib_deps option to specify the
; esp_websocket_client library only for the ESP-IDF framework.

lib_deps =
    ${env.lib_deps}
    esp_websocket_client=https://components.espressif.com/api/downloads/?object_type=component&object_id=dbc87006-9a4b-45e6-a6ab-b286174cb413

build_flags =
    ${env.build_flags}
    -D SENSESP_SSL_SUPPORT=1

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
; Board configurations follow

[esp32]

board = esp32dev
build_flags =
    ${env.build_flags}
    -D BUTTON_BUILTIN=0
    -D LED_BUILTIN=2

[esp32c3]

board = esp32-c3-devkitm-1
build_flags =
    ${env.build_flags}
    -D SENSESP_BUTTON_PIN=9
    -D PIN_RGB_LED=8
    ; Use the CDC USB port as Serial
    -DARDUINO_USB_MODE=1
    -DARDUINO_USB_CDC_ON_BOOT=1

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
; Permutations of platform and device.

[env:arduino_esp32]

extends = arduino, esp32
build_flags =
    ${arduino.build_flags}
    ${esp32.build_flags}

[env:pioarduino_esp32]

extends = pioarduino, esp32
build_flags =
    ${pioarduino.build_flags}
    ${esp32.build_flags}

[env:espidf_esp32]

extends = espidf, esp32
build_flags =
    ${espidf.build_flags}
    ${esp32.build_flags}

[env:arduino_esp32c3]

extends = arduino, esp32c3
build_flags =
    ${arduino.build_flags}
    ${esp32c3.build_flags}

[env:pioarduino_esp32c3]

extends = pioarduino, esp32c3
build_flags =
    ${pioarduino.build_flags}
    ${esp32c3.build_flags}

[env:espidf_esp32c3]

extends = espidf, esp32c3
build_flags =
    ${espidf.build_flags}
    ${esp32c3.build_flags}

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
; Individual board configurations

[env:shesp32]

extends = pioarduino, esp32
build_flags =
    ${pioarduino.build_flags}
    ${esp32.build_flags}

[env:halmet]

extends = pioarduino, esp32
board_build.partitions = default_8MB.csv

build_flags =
    ${pioarduino.build_flags}
    ${esp32.build_flags}

[env:halser]

extends = pioarduino, esp32c3

build_flags =
    ${pioarduino.build_flags}
    ${esp32c3.build_flags}

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
; Ethernet board configurations

[env:olimex_esp32_poe_iso]

extends = pioarduino
board = esp32-poe-iso
build_flags =
    ${pioarduino.build_flags}
    -D BUTTON_BUILTIN=-1
    -D LED_BUILTIN=2

[env:wt32_eth01]

extends = pioarduino
board = wt32-eth01
build_flags =
    ${pioarduino.build_flags}
    -D BUTTON_BUILTIN=-1

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
; Minimal Ethernet test — no SensESP, no BLE, just raw ETH + static IP

[env:eth_test]

extends = pioarduino
board = esp32dev
build_src_filter = -<*> +<../examples/eth_test/>
build_flags =
    ${pioarduino.build_flags}
lib_ignore =
    BLE
    BluetoothSerial
    SimpleBLE
    WiFiProv
    NimBLE-Arduino

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
; BLE gateway example (Ethernet + NimBLE, no WiFi radio contention)

[ble_gateway_common]

extends = pioarduino
board = esp32dev
board_build.partitions = min_spiffs.csv
lib_deps =
    ${pioarduino.lib_deps}
    h2zero/NimBLE-Arduino @ ^2.3.7
lib_ignore =
    BLE
    BluetoothSerial
    SimpleBLE
    WiFiProv
build_unflags =
    -D CORE_DEBUG_LEVEL=ARDUHAL_LOG_LEVEL_VERBOSE
build_flags =
    ${pioarduino.build_flags}
    -D CORE_DEBUG_LEVEL=ARDUHAL_LOG_LEVEL_INFO
    -D BUTTON_BUILTIN=-1
    -D LED_BUILTIN=2
    -D CONFIG_BT_NIMBLE_MAX_CONNECTIONS=4
    -D CONFIG_BT_NIMBLE_ROLE_CENTRAL=1
    -D CONFIG_BT_NIMBLE_ROLE_OBSERVER=1
    -D CONFIG_BT_NIMBLE_ROLE_PERIPHERAL=0
    -D CONFIG_BT_NIMBLE_ROLE_BROADCASTER=0

[env:signalk_ble_gateway]

extends = ble_gateway_common
build_flags =
    ${ble_gateway_common.build_flags}
    -D GATEWAY_HOSTNAME='"signalk-ble-gw"'

[env:signalk_ble_gateway_2]

extends = ble_gateway_common
build_flags =
    ${ble_gateway_common.build_flags}
    -D GATEWAY_HOSTNAME='"signalk-ble-gw-2"'

[env:signalk_ble_gateway_3]

extends = ble_gateway_common
build_flags =
    ${ble_gateway_common.build_flags}
    -D GATEWAY_HOSTNAME='"signalk-ble-gw-3"'

[env:signalk_ble_gateway_4]

extends = ble_gateway_common
build_flags =
    ${ble_gateway_common.build_flags}
    -D GATEWAY_HOSTNAME='"signalk-ble-gw-4"'

[env:signalk_ble_gateway_5]

extends = ble_gateway_common
build_flags =
    ${ble_gateway_common.build_flags}
    -D GATEWAY_HOSTNAME='"signalk-ble-gw-5"'
